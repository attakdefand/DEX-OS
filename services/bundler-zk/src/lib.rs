//! ZK Bundler Service for DEX-OS
//!
//! This crate provides ZK bundling for the DEX-OS.

/// ZK bundler service initialization
pub fn init() {
    println!("ZK bundler service initialized");
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
